import React from 'react';

const SelectedCategorie = () => {
	return (
		<div className="selectedCategorie">
			<h1>
				Top science(selectedCategory) news from Great Britain(country)
			</h1>
			<div>List all articles for selected categories, like TopNews</div>
		</div>
	);
};

export default SelectedCategorie;
