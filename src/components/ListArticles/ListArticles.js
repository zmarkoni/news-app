import React from 'react';
import Article from '../Article/Article';

const ListArticles = () => {
	return (
		<React.Fragment>
			<Article />
		</React.Fragment>
	);
};

export default ListArticles;
